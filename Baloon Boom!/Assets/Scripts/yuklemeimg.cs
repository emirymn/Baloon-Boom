using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class yuklemeimg : MonoBehaviour
{
    
    public UnityEngine.UI.Text yuklemetext;
    // Start is called before the first frame update
    void Start()
    {
       
    }

    // Update is called once per frame
    void Update()
    {
       
    }
    public void yndyukle()
    {
        
        yuklemetext.gameObject.SetActive(true);
    }
}
